# v3-only additions to /etc/rstudio/rsession.conf.
# Turn off the AI assistants, which would send code to external services.
# allow-* controls whether the integration can be enabled at all;
# *-enabled controls the feature itself.
allow-posit-assistant=0
posit-assistant-enabled=0
allow-copilot=0
copilot-enabled=0
